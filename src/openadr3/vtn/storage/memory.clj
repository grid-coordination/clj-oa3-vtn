(ns openadr3.vtn.storage.memory
  "Atom-backed in-memory storage implementation."
  (:require [com.stuartsierra.component :as component]
            [openadr3.vtn.storage :as storage]))

;; Implementation — VTN-jju
